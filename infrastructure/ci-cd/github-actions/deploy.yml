name: Push & Deploy

on:
  workflow_dispatch:
  release:
    types: [published]

jobs:
  push-ecr:
    name: Build and Push Docker Images
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
        
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2
        
      - name: Build, Tag, and Push
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "Placeholder for pushing images to ECR"
          # docker build -t $ECR_REGISTRY/frontend:$IMAGE_TAG ./frontend
          # docker push $ECR_REGISTRY/frontend:$IMAGE_TAG

  deploy-eks:
    name: Update EKS Cluster
    needs: push-ecr
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
        
      - name: Configure kubectl
        run: |
          echo "Placeholder for kubectl setup against AWS EKS"
          
      - name: Helm Upgrade
        run: |
          echo "Placeholder for helm deployment"
          # helm upgrade mfc-alytix ./infrastructure/helm/mfc-alytix
